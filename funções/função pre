void avr_pre(node *node)
{
	if(node != NULL)
	{
		printf("%c", *(unsigned char*) node->item);
		avr_pre(node->left);//aqui ficou uma recursão no formato pre ordem [C,E,D]
		avr_pre(node->rigth);
	}
}
